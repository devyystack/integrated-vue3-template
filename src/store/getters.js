const getters = {
    _addr: (state) => state.account._addr,
    name: (state) => state.account.name,
    _id: (state) => state.account._id,
    role: (state) => state.account.role,
  }
export default getters
  